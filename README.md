Factorial Program in C#
📌 Description

This is a simple C# console application that calculates the factorial of a number entered by the user.
The program uses a while loop to multiply numbers from 1 up to the given number and displays the final result.

🧮 What is Factorial?

The factorial of a number n is:

n! = n × (n-1) × (n-2) × ... × 1


Example:

5! = 5 × 4 × 3 × 2 × 1 = 120

